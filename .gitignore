# Documentation stays untracked until it has been reviewed.
# Un-ignore a file here to publish it.
*.md
!README.md

# Editor / OS noise
.DS_Store
*.swp
*~
.idea/
.vscode/

# Test scratch space
tests/tmp/

# Compiled CLI
/dist/

# Step 0 on the PC: the probe is built (make windows-capture), and what the
# capture produces is checked in as fixtures elsewhere, not from here
/tests/windows-capture/credread-probe.exe
/tests/windows-capture/out/

# Half-rendered artifacts (render_file cleans up after itself; belt and braces)
*.tmp.*
/tests/windows-capture/ptrbox.exe
/tests/windows-capture/smoke.ps1
